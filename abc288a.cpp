#include<bits/stdc++.h>
using namespace std;
using ll =long long;
#define rep(i,n) for(int i=0;i<(n);++i)
int main(){
    std::ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    int n;cin>>n;
    rep(i,n){
        ll a,b;cin>>a>>b;
        cout<<a+b<<endl;
    }
    return 0;
}