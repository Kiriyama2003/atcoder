#include<bits/stdc++.h>
using namespace std;
int main(void){
    string t; cin>>t; int n; cin>>n;
    vector<int>a(n);
    vector<vector<string>> s(n);
    for(int i=0;i<n;i++){
        cin>>a[i];
        s[i].resize(a[i]);
        for(int j=0;j<a[i];j++){
            cin>>s[i][j];
        }
    }

    vector<pair<string,int>> check()


    return 0;
}